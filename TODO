TODO file

### ALL DONE 

optional: 
1. write ransaclm in such a way that for the sequential case the optimal
   model is updated in each iteration (in case of an improvement) which 
   will probably make the code more memory efficient (?)